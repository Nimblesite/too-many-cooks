[meta]
name = Admin Status
description = Verify the admin status endpoint returns system state

[request]
GET {{baseUrl}}/admin/status

[assert]
status = 200
headers.Content-Type contains "application/json"
body.agents exists
body.locks exists
body.plans exists
body.messages exists
